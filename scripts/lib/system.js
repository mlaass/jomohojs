

define(['./jo',
        './Screen', 
        './input' , 
        './Loader', 
        './Surface'],
        function(jo, 
        		Screen, 
        		input, 
        		Loader, 
        		Log,
        		Surface){
	
		jo.system = {
				init: function(canvas, width, height, options){
					
				}
		};
	 return jo.system;	
});
